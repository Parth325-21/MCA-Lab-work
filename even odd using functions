#include <stdio.h>
int even(int);
int main() {
    int num1;
    printf("enter the a numbers");
    scanf("%d",&num1);
    even(num1);
}
int even(int n1)
{
    if(n1%2==0)
    printf("number is even");
    else
    printf("number is odd");
    return 0;
}
