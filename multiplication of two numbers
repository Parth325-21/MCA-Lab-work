#include <stdio.h>
int mul(int,int);
int main() {
    int num1,num2;
    printf("enter the two numbers");
    scanf("%d",&num1);
    scanf("%d",&num2);
    int sum = mul(num1,num2);
    printf("%d",sum);
    return 0;
}
int mul(int n1,int n2)
{
    return(n1*n2);
}
